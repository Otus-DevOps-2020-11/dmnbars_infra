name: 'Ansible Lint'

on:
  - push
  - pull_request

jobs:
  ansible-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: lint
        uses: ansible/ansible-lint-action@master
        with:
          targets: ansible
