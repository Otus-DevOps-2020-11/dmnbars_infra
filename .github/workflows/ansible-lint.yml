name: 'Ansible Lint'

on:
  - push
  - pull_request

jobs:
  ansible-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: debug
        run: |
          pwd
      - name: lint
        uses: addnab/docker-run-action@v2
        with:
          image: quay.io/ansible/toolset:latest
          options: -v ${PWD}/ansible:/app -w /app
          run: |
            ansible-lint
